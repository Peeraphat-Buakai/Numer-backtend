const result = async (x) => {
  var arr = []
  console.log('x =', x)
}

const test = (x) => {
  y_sum = (2 * x) - 3
  console.log("Y===", y_sum);
  data = {
    x: x,
    y: y_sum
  }
  return data
}

module.exports = { result }